TM_GIT = "/usr/bin/git"
tabSize = 2
softTabs = true
showInvisibles = true
softWrap = true
excludeInFileChooser = "{node_modules,_site}"
# include some dotfiles
include = "{*.*,.tm_properties,.htaccess,.gitconfig,.gitignore,.gitignore_global,.bash_profile,.jshintrc,.editorconfig}"

# Display the name of the home directory
windowTitle    = "$TM_DISPLAYNAME"

# files that need proper tabs
[ "{config,Makefile}" ]
softTabs = false
