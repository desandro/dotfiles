TM_GIT = "/usr/bin/git"
tabSize = 2
softTabs = true
showInvisibles = true
invisiblesMap = "~ "
softWrap = true
# fonts
fontName = "Consolas"
fontSize = 15

# include all files including dot files
include = "{*,.*}"
# exclude specifics
exclude = "{.DS_Store,.git,.esm-cache}"

excludeInFileChooser = "{node_modules,bower_components,_site}"
excludeInFolderSearch = "{node_modules,bower_components,_site}"

# Display the name of the home directory
windowTitle = "$TM_DISPLAYNAME"

# files that need proper tabs
[ "{config,Makefile}" ]
softTabs = false
